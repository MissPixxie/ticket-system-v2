"use client";

import { useState } from "react";
import { api } from "~/trpc/react";

export default function TicketsPage() {
  return (
    <main className="flex min-h-screen flex-row flex-wrap text-white">
      Tickets Page
    </main>
  );
}
